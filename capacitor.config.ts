import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.regibazar.driver',
  appName: 'RegiBazar',
  webDir: 'dist/regibazar-store/browser'
};

export default config;
